// Importing this will import @lexical/clipboard as a side-effect. We've added console.log statements to all react packages that get installed, so you can see which one ends up being used.

// Testing out an import that is actually declared in a/package.json
// TODO: import something from @lexical/markdown and log it.
