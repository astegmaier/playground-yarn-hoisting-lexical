# playground-yarn-hoisting-lexical

This is a playground to permit experimentation with yarn's hoisting behavior when multiple packages depend on different major versions of the same `lexical`-related dependency.
